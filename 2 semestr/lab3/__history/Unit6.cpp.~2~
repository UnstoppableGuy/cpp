//---------------------------------------------------------------------------

#pragma hdrstop

#include "Unit6.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)
void RecordList::Add(Record* new_item)
{
	if(!head)
		head = new_item;
	else
		tail -> next = new_item;
	tail = new_item;
}

void RecordList::Add(Record* new_item, int index)
{
	if(!head){
		head = new_item;
		tail = new_item;
	}
	else if()
	{

    }
	else
		tail -> next = new_item;
	tail = new_item;
}

/*void RecordList::print()
{
	Record* record = head;
	do{
			record -> print();
			record = record -> next;
	}while(record);
}*/
